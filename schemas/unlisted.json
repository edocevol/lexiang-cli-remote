{
  "_comment": "Unlisted but callable tools. tool_names is the source of truth (config-as-code). Run `lx tools sync-unlisted` to fetch schemas from MCP server and populate the tools dict.",
  "tool_names": [
    "entry_delete_entry",
    "file_list_revisions",
    "file_describe_revision",
    "file_revert_revision"
  ],
  "tools": {}
}